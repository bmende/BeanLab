apiVersion: apps/v1
kind: Deployment
metadata:
  name: makemkv
  namespace: default
  labels:
    app: makemkv
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: makemkv
  template:
    metadata:
      labels:
        app: makemkv
    spec:
      nodeSelector:
        node-role.beanlab/media: "true"
      containers:
        - name: makemkv
          image: jlesage/makemkv:latest
          ports:
            - name: web-ui
              containerPort: 5800
              protocol: TCP
          env:
            - name: USER_ID
              value: "1000"
            - name: GROUP_ID
              value: "1000"
          securityContext:
            privileged: true  # Required for MakeMKV to access optical drive SCSI subsystem
          volumeMounts:
            - name: config
              mountPath: /config
            - name: output
              mountPath: /output
            - name: optical-drive
              mountPath: /dev/sr0
            - name: scsi-generic
              mountPath: /dev/sg0
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              memory: 4Gi
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: makemkv-config
        - name: output
          hostPath:
            path: /srv/media/ripping
            type: DirectoryOrCreate
        - name: optical-drive
          hostPath:
            path: /dev/sr0
            type: BlockDevice
        - name: scsi-generic
          hostPath:
            path: /dev/sg0
            type: CharDevice
