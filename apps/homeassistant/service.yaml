apiVersion: v1
kind: Service
metadata:
  name: homeassistant
  namespace: default
spec:
  selector:
    app: homeassistant
  ports:
    - name: http
      port: 8123
      targetPort: http
      protocol: TCP
  type: ClusterIP
